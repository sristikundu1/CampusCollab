import { zodResolver } from '@hookform/resolvers/zod'; import { useEffect, useRef, useState } from 'react'; import { useForm } from 'react-hook-form'; import { Link, useLocation } from 'react-router-dom'; import { z } from 'zod'; import { FormField } from '../components/FormField.jsx'; import { Spinner } from '../components/Spinner.jsx'; import { AuthLayout } from '../layouts/AuthLayout.jsx'; import { apiError, authApi } from '../services/api.js';
const password=z.string().min(10,'Use at least 10 characters').regex(/[A-Z]/,'Include an uppercase letter').regex(/[a-z]/,'Include a lowercase letter').regex(/[0-9]/,'Include a number');const schema=z.object({password,confirmPassword:z.string()}).refine(v=>v.password===v.confirmPassword,{path:['confirmPassword'],message:'Passwords do not match'});
export function ResetPasswordPage(){const location=useLocation();const token=useRef(new URLSearchParams(location.search).get('token'));const [done,setDone]=useState(false);const {register,handleSubmit,setError,formState:{errors,isSubmitting}}=useForm({resolver:zodResolver(schema)});useEffect(()=>window.history.replaceState({},'',location.pathname),[location.pathname]);const submit=async(values)=>{if(!token.current)return setError('root',{message:'This reset link is missing its secure token.'});try{await authApi.reset({token:token.current,...values});setDone(true)}catch(e){setError('root',{message:apiError(e).message})}};return <AuthLayout eyebrow="Choose a new password" title="Secure your account" subtitle="Your new password will sign out any other active CampusCollab sessions.">{done?<div className="surface p-6 text-center"><p className="text-slate-600">Your password has been reset successfully.</p><Link to="/login" className="btn-primary mt-5 w-full">Sign in</Link></div>:<form className="space-y-5" onSubmit={handleSubmit(submit)}><FormField label="New password" type="password" autoComplete="new-password" error={errors.password?.message} {...register('password')}/><FormField label="Confirm new password" type="password" autoComplete="new-password" error={errors.confirmPassword?.message} {...register('confirmPassword')}/>{errors.root&&<p className="text-sm text-rose-700">{errors.root.message}</p>}<button className="btn-primary w-full" disabled={isSubmitting}>{isSubmitting?<Spinner label="Resetting"/>:'Reset password'}</button></form>}</AuthLayout>}

