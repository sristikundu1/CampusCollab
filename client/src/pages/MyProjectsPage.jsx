import { Plus } from 'lucide-react';import { useEffect,useState } from 'react';import { Link } from 'react-router-dom';import { ProjectCard } from '../components/projects/ProjectCard.jsx';import { AppShell } from '../layouts/AppShell.jsx';import { apiError,projectApi } from '../services/api.js';
export function MyProjectsPage(){const [projects,setProjects]=useState([]),[status,setStatus]=useState(''),[error,setError]=useState('');useEffect(()=>{void projectApi.mine(status?{status}:{}).then((r)=>setProjects(r.data.data.projects)).catch((e)=>setError(apiError(e).message))},[status]);return <AppShell><div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Collaboration workspace</p><h1 className="mt-2 text-3xl font-black">My Projects</h1><p className="mt-2 text-slate-600">Projects you own and teams you have joined.</p></div><Link className="btn-primary" to="/projects/new"><Plus size={17}/>Create project</Link></div><div className="mt-6 flex flex-wrap gap-2">{['','DRAFT','RECRUITING','ACTIVE','CANCELLED','ARCHIVED'].map((s)=><button key={s||'ALL'} onClick={()=>setStatus(s)} className={`rounded-full px-4 py-2 text-sm font-bold ${status===s?'bg-indigo-600 text-white':'border border-slate-200 bg-white text-slate-600'}`}>{s||'ALL'}</button>)}</div>{error?<p className="mt-6 rounded-xl bg-rose-50 p-4 text-rose-700">{error}</p>:projects.length?<div className="mt-7 grid gap-5 xl:grid-cols-2">{projects.map((p)=><ProjectCard key={p.id} project={p}/>)}</div>:<div className="surface mt-7 p-10 text-center"><h2 className="text-xl font-black">No projects in this view</h2><p className="mt-2 text-slate-600">Create a project or discover a team to join.</p><Link className="btn-secondary mt-5" to="/projects">Discover projects</Link></div>}</div></AppShell>}
