import { zodResolver } from '@hookform/resolvers/zod'; import { useState } from 'react'; import { useForm } from 'react-hook-form'; import { Link } from 'react-router-dom'; import { z } from 'zod'; import { FormField } from '../components/FormField.jsx'; import { Spinner } from '../components/Spinner.jsx'; import { AuthLayout } from '../layouts/AuthLayout.jsx'; import { apiError, authApi } from '../services/api.js';
export function ForgotPasswordPage(){const [sent,setSent]=useState('');const {register,handleSubmit,setError,formState:{errors,isSubmitting}}=useForm({resolver:zodResolver(z.object({email:z.string().email('Enter a valid email')}))});const submit=async({email})=>{try{const {data}=await authApi.forgot(email);setSent(data.data.message)}catch(e){setError('root',{message:apiError(e).message})}};return <AuthLayout eyebrow="Account recovery" title="Reset your password" subtitle="Enter your university email and we’ll send a secure, time-limited reset link.">{sent?<div className="surface p-6 text-sm leading-7 text-slate-600">{sent}<Link to="/login" className="btn-primary mt-5 w-full">Back to sign in</Link></div>:<form className="space-y-5" onSubmit={handleSubmit(submit)}><FormField label="University email" type="email" autoComplete="email" error={errors.email?.message} {...register('email')}/>{errors.root&&<p className="text-sm text-rose-700">{errors.root.message}</p>}<button className="btn-primary w-full" disabled={isSubmitting}>{isSubmitting?<Spinner label="Sending"/>:'Send reset link'}</button><Link className="block text-center text-sm font-semibold text-brand-600" to="/login">Back to sign in</Link></form>}</AuthLayout>}

