import { ArrowLeft,CalendarDays,CheckCircle2,Send,ShieldCheck,Users } from 'lucide-react';import { useEffect,useState } from 'react';import { Link,useNavigate,useParams } from 'react-router-dom';import { useAuth } from '../context/auth-context.js';import { useToast } from '../context/toast-context.js';import { MarketplaceLayout } from '../components/gigs/MarketplaceLayout.jsx';import { apiError,participationApi,projectApi } from '../services/api.js';
const label=(v)=>v.replaceAll('_',' ').toLowerCase().replace(/^./,(c)=>c.toUpperCase());
export function ProjectDetailsPage(){const {projectId}=useParams(),navigate=useNavigate(),{isAuthenticated}=useAuth(),{notify}=useToast();const [project,setProject]=useState(),[message,setMessage]=useState(''),[selected,setSelected]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{void projectApi.get(projectId).then((r)=>setProject(r.data.data.project)).catch((e)=>setError(apiError(e).status===404?'This project is not visible or no longer available.':apiError(e).message))},[projectId]);const join=async()=>{if(!isAuthenticated){notify('Sign in before requesting to join.','error');navigate('/login',{state:{from:`/projects/${projectId}`}});return}if(!selected||message.trim().length<1){notify('Choose a role and add a short message.','error');return}setBusy(true);try{await participationApi.requestJoin(projectId,selected,message.trim());notify('Join request sent.');setSelected('');setMessage('')}catch(e){notify(apiError(e).message,'error')}finally{setBusy(false)}};if(error)return <MarketplaceLayout><div className="surface mx-auto max-w-xl p-10 text-center"><h1 className="text-xl font-black">Project unavailable</h1><p className="mt-2 text-slate-600">{error}</p><Link className="btn-primary mt-5" to="/projects">Browse projects</Link></div></MarketplaceLayout>;if(!project)return <MarketplaceLayout><div className="mx-auto h-96 max-w-6xl animate-pulse rounded-3xl bg-slate-200"/></MarketplaceLayout>;return <MarketplaceLayout><div className="mx-auto max-w-6xl"><Link className="inline-flex items-center gap-2 text-sm font-bold text-indigo-700" to="/projects"><ArrowLeft size={16}/>Back to projects</Link><section className="mt-5 overflow-hidden rounded-3xl bg-[radial-gradient(circle_at_82%_15%,rgba(34,211,238,.32),transparent_28%),linear-gradient(135deg,#1e1b4b,#1d4ed8)] p-7 text-white sm:p-10"><div className="flex flex-wrap gap-2"><span className="rounded-full bg-white/15 px-3 py-1 text-xs font-black">{label(project.projectType)}</span><span className="rounded-full bg-white/15 px-3 py-1 text-xs font-black">{label(project.status)}</span></div><h1 className="mt-5 max-w-4xl text-3xl font-black sm:text-4xl">{project.title}</h1><p className="mt-5 flex items-center gap-2 text-sm font-bold text-blue-100"><ShieldCheck size={17}/>{project.owner.displayName} · University community member</p></section><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_350px]"><main className="space-y-6"><section className="surface p-7"><p className="eyebrow">Project mission</p><h2 className="mt-2 text-2xl font-black">What the team is building</h2><p className="mt-4 whitespace-pre-wrap leading-8 text-slate-600">{project.description}</p><div className="mt-6 flex flex-wrap gap-2">{project.skills.map((s)=><span key={s.id} className="rounded-xl bg-indigo-50 px-3 py-2 text-sm font-bold text-indigo-700"><CheckCircle2 className="mr-1 inline" size={15}/>{s.name}</span>)}</div></section><section><h2 className="text-2xl font-black">Open roles</h2><div className="mt-4 space-y-4">{project.openings.map((o)=><article key={o.id} className={`surface p-6 ${selected===o.id?'ring-2 ring-indigo-500':''}`}><div className="flex flex-wrap justify-between gap-3"><div><h3 className="text-lg font-black">{o.roleName}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{o.description}</p></div><span className="h-fit rounded-full bg-slate-100 px-3 py-1 text-xs font-black">{o.remainingCapacity} of {o.capacity} available</span></div><div className="mt-4 flex flex-wrap gap-2">{o.skills.map((s)=><span key={s.id} className="rounded-lg bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-600">{s.name}</span>)}</div>{!project.isOwner&&!project.isMember&&project.acceptingMembers&&o.status==='OPEN'&&<button className="btn-secondary mt-5" onClick={()=>setSelected(o.id)}><Send size={16}/>Request this role</button>}</article>)}</div></section></main><aside className="h-fit space-y-5 lg:sticky lg:top-24"><section className="surface p-6"><h2 className="text-lg font-black">Project snapshot</h2><dl className="mt-5 space-y-4 text-sm"><div className="flex gap-3"><Users className="text-indigo-600" size={18}/><div><dt className="text-slate-500">Recruitment</dt><dd className="font-bold">{project.acceptingMembers?'Accepting collaborators':'Paused'}</dd></div></div><div className="flex gap-3"><CalendarDays className="text-indigo-600" size={18}/><div><dt className="text-slate-500">Expected timeline</dt><dd className="font-bold">{project.expectedStartAt?new Date(project.expectedStartAt).toLocaleDateString():'Flexible'} — {project.expectedEndAt?new Date(project.expectedEndAt).toLocaleDateString():'Open'}</dd></div></div></dl>{project.isOwner&&<Link className="btn-primary mt-6 w-full" to={`/my-projects/${project.id}/manage`}>Manage project</Link>}</section>{selected&&!project.isOwner&&<section className="surface p-6"><h2 className="text-lg font-black">Join this team</h2><p className="mt-2 text-sm text-slate-500">Introduce yourself and explain how you can contribute.</p><textarea className="field mt-4 min-h-28" maxLength={3000} value={message} onChange={(e)=>setMessage(e.target.value)} placeholder="I would like to contribute because…"/><button className="btn-primary mt-4 w-full" disabled={busy} onClick={join}><Send size={16}/>{busy?'Sending…':'Send join request'}</button></section>}</aside></div></div></MarketplaceLayout>}
