import { cleanup,render,screen,waitFor } from '@testing-library/react';import userEvent from '@testing-library/user-event';import { afterEach,beforeEach,describe,expect,it,vi } from 'vitest';import { MemoryRouter,Route,Routes } from 'react-router-dom';import { AuthContext } from '../context/auth-context.js';import { ToastContext } from '../context/toast-context.js';import { ProjectDetailsPage } from './ProjectDetailsPage.jsx';import { ProjectsPage } from './ProjectsPage.jsx';import { ParticipationInboxPage } from './ParticipationInboxPage.jsx';
const mocks=vi.hoisted(()=>({list:vi.fn(),get:vi.fn(),join:vi.fn(),myJoins:vi.fn(),myInvites:vi.fn(),joinAction:vi.fn(),inviteAction:vi.fn(),notify:vi.fn(),confirm:vi.fn()}));vi.mock('../services/api.js',()=>({projectApi:{list:mocks.list,get:mocks.get},participationApi:{requestJoin:mocks.join,myJoins:mocks.myJoins,myInvitations:mocks.myInvites,joinAction:mocks.joinAction,invitationAction:mocks.inviteAction},apiError:(e)=>({status:e?.response?.status,message:e?.message||'Request failed'})}));vi.mock('../lib/confirm-action.js',()=>({confirmAction:mocks.confirm}));
const ID='aaaaaaaaaaaaaaaaaaaaaaaa',OPEN='bbbbbbbbbbbbbbbbbbbbbbbb',REQUEST='cccccccccccccccccccccccc';const project={id:ID,title:'Accessible campus research portal',description:'A professional collaboration project for sharing accessible student research.',projectType:'RESEARCH',skills:[{id:'d'.repeat(24),name:'React'}],visibility:'PLATFORM',expectedStartAt:null,expectedEndAt:null,openings:[{id:OPEN,roleName:'Frontend contributor',description:'Build and test the accessible user interface.',skills:[{id:'d'.repeat(24),name:'React'}],capacity:2,filledCount:0,remainingCapacity:2,status:'OPEN'}],acceptingMembers:true,status:'RECRUITING',owner:{id:'e'.repeat(24),displayName:'Project Owner'},isOwner:false,isMember:false};const response=(data)=>Promise.resolve({data:{data,meta:{pagination:{hasMore:false}}}});const auth={loading:false,isAuthenticated:true,user:{id:'f'.repeat(24),email:'student@bscse.uiu.ac.bd',profile:{displayName:'Student'}},logout:vi.fn()};function show(ui,path){return render(<MemoryRouter initialEntries={[path]}><AuthContext.Provider value={auth}><ToastContext.Provider value={{notify:mocks.notify}}>{ui}</ToastContext.Provider></AuthContext.Provider></MemoryRouter>)}
beforeEach(()=>{mocks.list.mockImplementation(()=>response({projects:[project]}));mocks.get.mockImplementation(()=>response({project}));mocks.join.mockImplementation(()=>response({joinRequest:{id:REQUEST,status:'PENDING'}}));mocks.myJoins.mockImplementation(()=>response({joinRequests:[{id:REQUEST,project:{id:ID,title:project.title},opening:{roleName:'Frontend contributor'},message:'I can help.',status:'PENDING'}]}));mocks.myInvites.mockImplementation(()=>response({invitations:[]}));mocks.confirm.mockResolvedValue(true);mocks.joinAction.mockImplementation(()=>response({result:{}}))});afterEach(()=>{cleanup();vi.clearAllMocks()});
describe('Projects collaboration workflow',()=>{it('shows active projects as professional discovery cards',async()=>{show(<ProjectsPage/>,'/projects');expect(await screen.findByRole('heading',{name:project.title})).toBeInTheDocument();expect(screen.getByText(/0\/2 roles filled/)).toBeInTheDocument();expect(screen.getByRole('link',{name:/View/})).toHaveAttribute('href',`/projects/${ID}`)});it('lets an authenticated student select a role and send a join request',async()=>{const user=userEvent.setup();show(<Routes><Route path="/projects/:projectId" element={<ProjectDetailsPage/>}/></Routes>,`/projects/${ID}`);await user.click(await screen.findByRole('button',{name:'Request this role'}));await user.type(screen.getByPlaceholderText('I would like to contribute because…'),'I can build and test accessible React components.');await user.click(screen.getByRole('button',{name:'Send join request'}));await waitFor(()=>expect(mocks.join).toHaveBeenCalledWith(ID,OPEN,expect.stringContaining('accessible')));expect(mocks.notify).toHaveBeenCalledWith('Join request sent.')});it('shows the request lifecycle and confirms withdrawal without browser confirmation',async()=>{const user=userEvent.setup();show(<ParticipationInboxPage type="joins"/>,'/join-requests');expect(await screen.findByRole('heading',{name:project.title})).toBeInTheDocument();await user.click(screen.getByRole('button',{name:'Withdraw request'}));await waitFor(()=>expect(mocks.confirm).toHaveBeenCalled());expect(mocks.joinAction).toHaveBeenCalledWith(REQUEST,'withdraw')});it('renders an empty invitation inbox clearly',async()=>{show(<ParticipationInboxPage type="invitations"/>,'/invitations');expect(await screen.findByText('Nothing here yet')).toBeInTheDocument()})});
