import { MailCheck } from 'lucide-react'; import { useEffect, useRef, useState } from 'react'; import { Link, useLocation } from 'react-router-dom'; import { Spinner } from '../components/Spinner.jsx'; import { AuthLayout } from '../layouts/AuthLayout.jsx'; import { apiError, authApi } from '../services/api.js';
export function VerifyEmailPage(){const location=useLocation();const tokenRef=useRef(new URLSearchParams(location.search).get('token'));const [state,setState]=useState(tokenRef.current?'loading':'waiting');const [message,setMessage]=useState(location.state?.message||'We sent a verification link to your university inbox.');const [email,setEmail]=useState(location.state?.email||'');useEffect(()=>{if(!tokenRef.current)return;window.history.replaceState({},'',location.pathname);authApi.verify(tokenRef.current).then(({data})=>{setMessage(data.data.message);setState('success')}).catch((error)=>{setMessage(apiError(error).message);setState('error')})},[location.pathname]);const resend=async()=>{try{const {data}=await authApi.resend(email);setMessage(data.data.message)}catch(error){setMessage(apiError(error).message)}};return <AuthLayout eyebrow="Verify your identity" title="Check your university email" subtitle="Verification unlocks the trusted CampusCollab community."><div className="surface p-7 text-center"><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-brand-50 text-brand-600"><MailCheck size={30}/></span><p className={`mt-5 text-sm leading-6 ${state==='error'?'text-rose-700':'text-slate-600'}`}>{state==='loading'?<Spinner label="Verifying your email"/>:message}</p>{state==='success'&&<Link to="/login" className="btn-primary mt-6 w-full">Continue to sign in</Link>}{!tokenRef.current&&<div className="mt-6 space-y-3"><input className="field" type="email" placeholder="you@university.edu" value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn-secondary w-full" disabled={!email} onClick={resend}>Resend verification email</button></div>}{state==='error'&&<Link to="/register" className="btn-secondary mt-6 w-full">Return to registration</Link>}</div></AuthLayout>}

